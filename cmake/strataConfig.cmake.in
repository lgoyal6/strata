@PACKAGE_INIT@

# strata links Threads publicly, so a consumer's find_package(strata) has to
# resolve it too or the link fails with undefined pthread symbols.
include(CMakeFindDependencyMacro)
find_dependency(Threads)

include("${CMAKE_CURRENT_LIST_DIR}/strataTargets.cmake")

check_required_components(strata)
